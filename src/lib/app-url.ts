// Canonical public origin for the production app. Used for invite links and
// other URLs sent to external recipients so they never leak preview/editor URLs.
export const APP_URL = "https://globalcontractor.app";
